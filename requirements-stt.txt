# Dependencies for the STT microservice (whisper_service.py), which runs in its
# OWN venv (sttenv) because CohereLabs/cohere-transcribe-arabic-07-2026 needs
# transformers>=5.4 while the VoxCPM2 TTS engine (main venv) pins an older transformers.
#
# Install into a dedicated venv:
#   python -m venv sttenv
#   source sttenv/Scripts/activate
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-stt.txt
#
# The model is gated (~4.13 GB). Once, before first run:
#   hf auth login
#   # accept the license at https://huggingface.co/CohereLabs/cohere-transcribe-arabic-07-2026
#   HF_HUB_DISABLE_XET=1 hf download CohereLabs/cohere-transcribe-arabic-07-2026

# torch is installed separately from the CUDA index (see above) — not listed here.
transformers>=5.4.0
accelerate
sentencepiece
protobuf
soundfile
librosa
fastapi
uvicorn[standard]
python-multipart
huggingface_hub
hf_transfer
