fastapi
uvicorn[standard]
requests
pydantic
python-multipart
voxcpm
soundfile
# NOTE: speech-to-text is NOT installed here. It runs in a separate venv/process
# (whisper_service.py) — see requirements-stt.txt — because its transformers>=5.4
# conflicts with the TTS engine's pinned transformers.