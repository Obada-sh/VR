# Copy to .env (gitignored). The justfile loads it automatically, so `just main`
# picks these up without you exporting anything.

# --- LLM ----------------------------------------------------------------------
# Free OpenAI-compatible providers, catalogued at
# https://github.com/cheahjs/free-llm-api-resources
#
# This is an ordered FAILOVER CHAIN, not a single choice: each request tries
# them left to right until one answers. Free tiers go down, hit their daily cap,
# and retire model IDs, so list at least two. Providers whose key is missing are
# skipped, so leaving extras in the chain costs nothing.
LLM_PROVIDERS=google,groq,cerebras

GOOGLE_API_KEY=...          # aistudio.google.com/apikey
GROQ_API_KEY=...            # console.groq.com/keys
CEREBRAS_API_KEY=...        # cloud.cerebras.ai
# OPENROUTER_API_KEY=...    # openrouter.ai/keys
# MISTRAL_API_KEY=...       # console.mistral.ai
# OPENCODE_API_KEY=sk-...   # the original paid provider

# Optional — override one provider's model. Free line-ups change often; run
# `just models` to see what each key can actually reach.
# GOOGLE_MODEL=gemini-2.5-flash
# GROQ_MODEL=llama-3.1-8b-instant

# --- STT ----------------------------------------------------------------------
# Optional — only if the STT service runs somewhere other than the default.
# WHISPER_SERVICE_URL=http://127.0.0.1:8001
